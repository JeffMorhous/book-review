<h1><%= params[:category] %></h1>
<% if @books.count == 0 %>
  <h3>There are no books in this category, feel free to add one 💙</h3>
<% else %>
  <div class="row">
    <% @books.each do |b|%>
      <div class="col-md-3">
        <a href="/books/<%= b.id %>"><%= image_tag  b.book_img.url(:book_index), class: "book" %></a>
      </div>
    <% end %>
  </div>
<% end %>
